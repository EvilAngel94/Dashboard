CREATE TABLE Interactable (
    id int PRIMARY KEY,
    redirectionUrl VARCHAR(255),
    placeholderSrc VARCHAR(255),
    src VARCHAR(255),
    slug VARCHAR(255),
    gif VARCHAR(255),
    title VARCHAR(255)
)